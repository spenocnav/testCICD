import { NavifaultManagementClient } from './management-client';

export const metadata = { title: 'Bandeja de gestión · Navifault' };

export default function NavifaultManagementPage() {
  return <NavifaultManagementClient />;
}
