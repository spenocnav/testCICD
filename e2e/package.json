{
  "name": "portal-clientes-e2e",
  "version": "0.1.0",
  "private": true,
  "description": "Pruebas E2E (Playwright) del Portal Clientes. Standalone, fuera del workspace pnpm.",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui",
    "report": "playwright show-report",
    "codegen": "playwright codegen http://100.108.186.115:3000"
  },
  "devDependencies": {
    "@playwright/test": "1.50.1"
  }
}
